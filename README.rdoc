= Rails 4 application with Bootstrap


== Quick start

Three quick start options are available:

* Clone the repo: `git clone https://github.com/veera/rails4_bootstrap.git`.
* Go to application directory `cd rails4_bootstrap`.
* Start the server `rails s`.

== Checkout Bootstrap example templates

jumbotron-narrow - http://localhost:3000
